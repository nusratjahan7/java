console.log( 2 === '2');
// compares the data type first. if data types are different, it returns false. if data types are same, it compares the values and returns true or false accordingly.

console.log(2 == 2);
console.log(2 == '2');
console.log(1 == true);
console.log(0 == false);
// double equals does type coercion. compares the values after converting them to a common type. if the values are equal after the coercion, it returns true. if the values are not equal after type coercion, it returns false.

